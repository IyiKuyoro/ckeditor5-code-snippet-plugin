{
  "name": "ckeditor-code-snippet-plugin",
  "version": "0.0.13",
  "description": "A simple CKEditor5 plugin that helps create preformatted text in the rich text editor.",
  "main": "./dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "babel src -d dist",
    "lint": "eslint --fix ./src",
    "prepare": "npm run clean && mkdir dist && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "keywords": [
    "ckeditor",
    "plugin",
    "code",
    "pre",
    "snippet"
  ],
  "author": "Opeoluwa Iyi-Kuyoro",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@babel/cli": "^7.7.4",
    "@babel/core": "^7.7.4",
    "@babel/preset-env": "^7.7.4",
    "eslint": "^6.7.1",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2"
  }
}
